\newglossaryentry{workspace}
{
    name=workspace,
    description={%
        % Workspaces are created each time a QR Code scanned for the first time. Workspace represents the data collection
        Workspaces combine data and models together. Each workspace has only one set of data and multiple models on that data.
    }
}